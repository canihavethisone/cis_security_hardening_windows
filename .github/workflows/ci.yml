---
name: CI

on:
  push:
#    branches:
#      - main
#      - feature/*
    paths:
    - '.github/**'
    - 'hiera.yaml'
    - 'data/**'
    - 'manifests/**'
    - 'files/**'
    - 'spec/**'
    - 'lib/**'
    - 'Gemfile'
  workflow_dispatch:
    paths:
    - '.github/**'
    - 'hiera.yaml'
    - 'data/**'
    - 'manifests/**'
    - 'files/**'
    - 'spec/**'
    - 'lib/**'
    - 'Gemfile'
  pull_request:
    paths:
    - 'hiera.yaml'
    - 'data/**'
    - 'manifests/**'
    - 'spec/**'
    - 'lib/**'

jobs:
  puppet:
    name: Puppet
    uses: voxpupuli/gha-puppet/.github/workflows/beaker.yml@v4
